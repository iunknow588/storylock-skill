# Android 真机验收记录

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1 |
| 日期 | 2026-06-22 |
| 适用范围 | Android Host 原型真机运行、绑定、relay、`/permission-summary` 与脱敏返回 |
| 目的 | 记录脚本无法替代的 Android 真机安装、唤起、本地确认和远程网关联调结果 |

## 1. 验收入口

```powershell
scripts\android\check_device_loop.ps1 `
  -ApkPath release\app\android\storylock-android-host-0.1.0-1-debug.apk `
  -GatewayBaseUrl https://yian.cdao.online `
  -IdentityId android-demo-001 `
  -PackageKind debug `
  -ReleaseChannel internal `
  -DeepLink "storylock-host://bind?gateway_url=...&binding_token=...&identity_id=android-demo-001&preferred_mode=relay_url" `
  -AndroidHostPort 4500 `
  -LocalForwardPort 14500 `
  -SharedSecret "replace-with-strong-shared-secret" `
  -ReportPath docs\test\Android真机验收记录_20260622.local.md
```

真机检查说明见 `docs/ref/10-Android真机闭环检查.md`。

## 2. 前置自动检查

```powershell
npm run test:android-readiness
npm run test:delivery
```

2026-06-22 自动化复核：`npm run test:android-readiness` 已通过，当前仅说明 Android 代码、资源、Manifest、Keystore、BiometricPrompt、题集和 `/permission-summary` asset 准备度通过；不等同于真机验收完成。

2026-06-22 本机探测：已运行 `scripts\android\check_device_loop.ps1 -ReportPath .temp\android-device-loop-report.local.md`。脚本成功启动 adb daemon，但 `adb devices` 未发现已连接设备，因此 APK 安装、deep link 唤起、relay 和真机 `/permission-summary` 实测均被阻塞。脚本现已支持连接设备后的 `adb forward tcp:14500 tcp:4500`，并会直接检查 Android Host 的 `/health` 与 `/permission-summary`。

## 3. 环境记录

| 项目 | 记录 |
| --- | --- |
| 设备型号 | 待验收 |
| Android 版本 / SDK | 待验收 |
| APK 路径 | 待验收 |
| APK SHA-256 | 待验收 |
| packageKind | 待验收 |
| releaseChannel | 待验收 |
| GatewayBaseUrl | 待验收 |
| IdentityId | 待验收 |

本机探测记录：

| 项目 | 记录 |
| --- | --- |
| adb | 可启动 daemon |
| 已连接设备 | 未发现 |
| 本机报告 | `.temp/android-device-loop-report.local.md` |
| 阻塞原因 | 当前机器未连接 Android 设备，且未提供 APK、DeepLink、GatewayBaseUrl |

## 4. 真机检查项

| 编号 | 检查项 | 通过标准 | 结果 | 记录 |
| --- | --- | --- | --- | --- |
| AND-01 | `adb devices` | 目标设备显示为 `device` | 阻塞 | 2026-06-22 本机探测未发现设备 |
| AND-02 | APK 安装 | `adb install -r` 成功，设备上出现 Android Host | 待验收 |  |
| AND-03 | deep link 唤起 | `storylock-host://bind?...` 能唤起宿主并写入绑定状态 | 待验收 |  |
| AND-04 | 注册与 relay | 宿主注册成功，relay poll/respond 可持续成功 | 待验收 |  |
| AND-05 | `/permission-summary` | Android 宿主经 `adb forward` 返回脱敏权限摘要，items 数量与 asset 一致 | 待验收 |  |
| AND-06 | `requestSignature` | 弹出本地 challenge 与确认，返回结果不暴露 `privateKey` 或 `signingKeyBytes` | 待验收 |  |
| AND-07 | `requestPasswordFill` | 弹出本地 challenge 与确认，返回结果不明文暴露 `password` 到远程网关响应 | 待验收 |  |
| AND-08 | 取消路径 | challenge 或 BiometricPrompt 取消时返回结构化错误 | 待验收 |  |
| AND-09 | 失败与锁定 | 连续错误输入触发预期失败或临时锁定 | 待验收 |  |

## 5. 当前结论

当前仅可表述为：Android Host 原型代码准备度和资源接线自动检查已通过。

在 AND-01 到 AND-09 全部记录为通过前，不应表述为：Android 真机运行和 `/permission-summary` 实测已完成。
