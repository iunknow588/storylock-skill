# Linux 桌面与 WSL 验收记录

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1 |
| 日期 | 2026-06-22 |
| 适用范围 | Linux Host 原型、WSL 打包、Linux 桌面集成、Secret Service 真环境 |
| 目的 | 记录脚本无法替代的 Linux 桌面安装、Secret Service、systemd user service 和运行态表现 |

## 1. 验收入口

自动检查：

```powershell
npm run test:linux-host
npm run test:linux-package
npm run test:linux-desktop
npm run test:linux-wsl
npm run diagnose:linux-secret-service:wsl
```

WSL 打包：

```powershell
npm run package:linux-host:wsl
```

真实 Linux 环境运行：

```bash
STORYLOCK_LINUX_USE_PLATFORM_SECRET_STORE=1 \
STORYLOCK_LINUX_DEVELOPMENT_MODE=0 \
node src/host/linux-host/server.mjs
```

## 2. 前置自动检查

2026-06-22 自动化复核：`npm run test:linux-host` 已通过，覆盖 health、`/permission-summary`、24 题题库导入、12 格 verify、authorize、execute、revoke。

仍需真实 Linux 桌面或 WSL 环境记录 Secret Service、安装包、desktop entry 与 systemd user service 的实际表现。

2026-06-22 WSL 复核：已在 `Ubuntu-22.04` 中运行 `npm run package:linux-host:wsl`，脚本通过 nvm 使用 Node.js `v24.17.0` 并成功生成 `.tar.gz` 与 `.deb` 原型包。随后 `npm run test:linux-package` 与 `npm run test:linux-desktop` 通过，manifest 标记 `debBuilt=true`。Secret Service 生产检查仍阻塞：WSL 默认 `node` 为 `v12.22.9`，诊断脚本选择 `v24.17.0` 后确认当前 WSL 环境未安装 `secret-tool`；`npm run diagnose:linux-secret-service:wsl` 已生成 `.temp/linux-secret-service-wsl-report.local.md`，其中 `SECRET_TOOL=missing`，`CHECK_SECRET_STORE_EXIT=1`。

## 3. 环境记录

| 项目 | 记录 |
| --- | --- |
| Linux 发行版 / WSL 发行版 | 待验收 |
| Node.js 版本 | WSL 默认 `v12.22.9`；打包脚本通过 nvm 使用 `v24.17.0` |
| `secret-tool` 可用性 | 阻塞，`.temp/linux-secret-service-wsl-report.local.md` 记录 `SECRET_TOOL=missing` |
| Secret Service 会话 | 阻塞，需安装 libsecret tools 并确认用户会话 Secret Service |
| 包类型 | `tar.gz`、`deb` |
| 包路径 | `release/app/linux/yian-linux-host-0.1.0-1-prototype.tar.gz`、`release/app/linux/yian-linux-host-0.1.0-1-prototype.deb` |
| SHA-256 | `tar.gz`: `sha256:6f932654cdd9fa7c79786578e1a923ccd6b060917979de69ac0c00e27ccba174`; `deb`: `sha256:08287b75b6ea383f34a222130ae37f1025b004c09817104cb75f526296210f81` |
| releaseChannel | `prototype` |

## 4. 桌面与 WSL 检查项

| 编号 | 检查项 | 通过标准 | 结果 | 记录 |
| --- | --- | --- | --- | --- |
| LIN-01 | WSL 打包 | `npm run package:linux-host:wsl` 生成 `.deb` 与 `tar.gz`，manifest 记录 artifact | 通过 | 2026-06-22 使用 Ubuntu-22.04 与 nvm Node.js v24.17.0 |
| LIN-02 | 包内容 | `npm run test:linux-package` 通过 | 通过 | `debEntries=88`，`wslDistro=Ubuntu-22.04` |
| LIN-03 | 桌面集成材料 | `npm run test:linux-desktop` 通过，desktop entry、systemd user unit、Debian control 一致 | 通过 | `debBuilt=true` |
| LIN-04 | WSL 脚本守门 | `npm run test:linux-wsl` 通过 | 通过 | 2026-06-22 已通过 |
| LIN-05 | Secret Service | 真 Linux 环境中 `secret-tool` 与 Secret Service 可用 | 阻塞 | `npm run diagnose:linux-secret-service:wsl` 已确认 `secret-tool` 缺失 |
| LIN-06 | 生产模式启动 | `STORYLOCK_LINUX_USE_PLATFORM_SECRET_STORE=1` 且 development mode 关闭时服务启动 | 待验收 |  |
| LIN-07 | `/health` | 返回 `node-linux-prototype`，题库和 StoryLock package 状态明确 | 待验收 |  |
| LIN-08 | `/permission-summary` | 返回脱敏权限摘要，不含答案、密码、私钥或签名字节 | 待验收 |  |
| LIN-09 | systemd user service | 安装后 user service 可 enable/start/status | 待验收 |  |
| LIN-10 | desktop entry | 桌面环境能识别并启动 Yian Linux Host | 待验收 |  |

## 5. 当前结论

当前仅可表述为：Linux Host 原型自动闭环、包内容和桌面集成材料具备可检查入口。

在 LIN-01 到 LIN-10 全部记录为通过前，不应表述为：Linux 桌面或 WSL 环境运行验收已完成。
