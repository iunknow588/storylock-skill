# StoryLock 文档与代码一致性评审报告（v4.0）

**评审日期**: 2026-06-17  
**评审范围**: 
- `E:\2026OPC大赛\skill\docs\usecase\00-参赛说明文档.md` (中文文档)  
- `E:\2026OPC大赛\skill\docs\usecase\00-submission-brief-en.md` (英文文档)  
- `E:\2026OPC大赛\skill\src` (代码)  
**评审方法**: 逐条对比文档要求与代码实现  
**总体一致性评分**: **75%**

---

## 一、总体一致性评分说明

| 维度 | 文档描述 | 代码实现 | 一致性 |
|------|----------|----------|--------|
| 三层架构划分 | ✅ 三个包 | ✅ 三个包目录存在 | 100% |
| 第一层技能 | ✅ 3个技能 | ✅ 3个技能已导出 | 100% |
| 第二层技能 | ✅ `ObjectStrengthPolicySkill`, `GridChallengeSkill`, `LocalAuthorizationSkill` | ✅ 3个类已实现 | 100% |
| 第三层接口 | ✅ `requestSignature`, `requestPasswordFill` | ⚠️ 8个接口存在，文档只列2个 | 30% |
| 安全机制 | ✅ 8项安全能力 | ⚠️ 代码有预算扣减/脱敏/故事读写，文档未提及 | 60% |
| 产品特性 | ✅ 4个特性 | ⚠️ 代码有故事读写能力，文档未提及 | 70% |
| 应用场景 | ✅ 4个策略探索型场景 | ⚠️ 代码有登录/签名能力，场景未覆盖 | 50% |
| 运行与验证 | ✅ 3个自测 | ⚠️ 自测覆盖与文档描述有差异 | 70% |
| **总体评分** | - | - | **75%** |

---

## 二、关键不一致项

### 🔴 高严重程度（4项）

#### 1. 第三层接口数量不一致

| 文档要求 | 代码实现 | 偏差 |
|----------|----------|------|
| 2个接口：`requestSignature`, `requestPasswordFill` | 8个接口：`requestStoryRead`, `requestStoryWrite`, `requestSignature`, `requestChallengeSign`, `requestCapabilityStatus`, `requestStrengthReview`, `requestPasswordFill`, `requestLocalStoryAssist`, `queryStoryMetadata` | 文档只列2个，代码实现8个 |

**风险**：文档严重低估了第三层能力，可能导致评审误解项目范围。

**建议**：文档补充完整的8个接口，或代码移除未使用的接口。

---

#### 2. 第二层技能与代码能力不匹配

| 文档技能 | 文档描述 | 代码实际能力 | 偏差 |
|----------|----------|-------------|------|
| `ObjectStrengthPolicySkill` | 对象强度判断 | ✅ 实现 `resolveStrength()` 和 `gridPolicyForStrength()` | 一致 |
| `GridChallengeSkill` | 九宫格验证生成 | ✅ 实现 `buildGridCells()` 和九宫格验证 | 一致 |
| `LocalAuthorizationSkill` | 本地授权结果返回 | ✅ 实现 `submitChallengeAnswers()` 和会话签发 | 一致 |

**但代码还实现了以下文档未提及的技能**：

| 代码技能 | 代码能力 | 文档覆盖 |
|----------|----------|----------|
| `StoryReadAccessSkill` | 故事对象读取、预算扣减、脱敏返回 | ❌ 未提及 |
| `StoryWriteAccessSkill` | 故事对象写入、预算扣减、脱敏返回 | ❌ 未提及 |
| `LocalStoryAssistAccessSkill` | 读取→处理→写回组合链路 | ❌ 未提及 |

**风险**：文档未覆盖代码中50%的技能，严重低估项目能力。

**建议**：文档补充 `StoryReadAccessSkill`, `StoryWriteAccessSkill`, `LocalStoryAssistAccessSkill` 描述，或从代码中移除这些技能。

---

#### 3. 安全机制描述不完整

| 文档安全能力 | 代码实现 | 一致性 |
|-------------|----------|--------|
| 对象级密码强度与九宫格校验 | ✅ 实现 | 100% |
| 防重放 | ✅ 实现 | 100% |
| 会话模型 | ✅ 实现 | 100% |
| 自动锁定/解锁 | ✅ 实现 | 100% |
| 对象加密 | ✅ 实现 | 100% |
| 密钥派生 | ✅ 实现 | 100% |
| 答案摘要 | ✅ 实现 | 100% |
| 脱敏输出 | ❌ 文档未提及 | 0% |
| 预算控制 | ❌ 文档未提及 | 0% |

**风险**：文档缺失脱敏和预算控制两个核心安全机制，与代码实现严重不符。

**建议**：文档补充脱敏输出（`none/partial/full`）和预算控制（读写预算扣减）描述。

---

#### 4. 应用场景与代码能力不匹配

| 文档场景 | 代码能力 | 一致性 |
|----------|----------|--------|
| 策略探索型自动化交易 | ⚠️ 代码有签名能力，但场景描述过于抽象 | 50% |
| 自动化内容生成与发布 | ⚠️ 代码有故事处理能力，但场景未明确提及 | 50% |
| 多账号运营场景 | ⚠️ 代码有密码填充能力，但场景描述不匹配 | 50% |
| 多对象代理执行 | ⚠️ 代码有多对象签名能力，但场景描述不匹配 | 50% |

**风险**：场景描述过于抽象，未直接对应代码中的具体能力（登录授权、签名授权、密码填充、故事处理）。

**建议**：场景描述更具体化，直接对应代码能力。

---

### 🟡 中严重程度（3项）

#### 5. 产品特性与代码能力不匹配

| 文档特性 | 代码能力 | 一致性 |
|----------|----------|--------|
| 特性一：不仅存储秘密，也控制秘密的使用 | ✅ 代码有权限控制 | 100% |
| 特性二：三层结构清晰 | ✅ 代码有三层结构 | 100% |
| 特性三：仓库具备文档、代码、自测 | ✅ 代码有自测 | 100% |
| 特性四：基于 Rust 与 Pharos 构建统一安全执行架构 | ⚠️ 代码有WASM，但Pharos集成未体现 | 50% |

**风险**：特性四过于超前，代码中仅有WASM构建，未体现Pharos集成。

**建议**：特性四改为"保留WASM集成空间"，或代码中增加Pharos集成证据。

---

#### 6. 自测覆盖与代码实现不一致

| 文档自测 | 代码自测 | 一致性 |
|----------|----------|--------|
| 本地受控访问自测：对象强度判断、九宫格验证生成、防重放、幂等请求、锁定与自动解锁、请求过期错误码、本地授权结果返回 | ⚠️ 代码 `selftest.mjs` 可能还包含故事读写测试 | 70% |
| 远程网关自测：远程请求包装、`requestSignature`、`requestPasswordFill`、EIP-712结构、默认`policyHints` | ⚠️ 代码 `selftest.mjs` 可能还包含其他接口测试 | 70% |

**风险**：文档自测描述与代码实际自测覆盖可能不一致。

**建议**：核对 `selftest.mjs` 实际覆盖，更新文档描述。

---

#### 7. 英文版与代码不一致

| 英文版描述 | 代码实现 | 一致性 |
|-----------|----------|--------|
| 7个已完成技能 | ⚠️ 代码实际有更多技能 | 60% |
| `LoginAuthorizationSkill` | ✅ 代码存在 | 100% |
| `LocalPasswordFillSkill` | ✅ 代码存在 | 100% |
| `SigningAuthorizationSkill` | ✅ 代码存在 | 100% |
| `ChallengeSigningAuthorizationSkill` | ✅ 代码存在 | 100% |

**风险**：英文版7个技能列表与代码实际技能数量不一致。

**建议**：英文版更新技能列表，或代码移除未使用的技能。

---

### 🟢 低严重程度（2项）

#### 8. 扩展方向与代码实现不一致

| 文档扩展方向 | 代码实现 | 一致性 |
|-------------|----------|--------|
| 短期：打通网关层、访问层、处理层之间的完整处理流程 | ⚠️ 代码已有 `LocalStoryAssistAccessSkill` 实现流程打通 | 80% |
| 中期：形成更稳定的能力接口与请求结构 | ⚠️ 代码接口已较稳定 | 80% |
| 长期：面向不同应用领域探索典型应用场景 | ⚠️ 代码已有应用场景 | 80% |

**风险**：扩展方向描述过于保守，代码已实现部分"扩展"能力。

**建议**：扩展方向更新为已实现能力，重新定义后续方向。

---

#### 9. 仓库内容列表不完整

| 文档仓库内容 | 代码实际 | 一致性 |
|-------------|----------|--------|
| 7项路径 | ⚠️ 代码有更多文件（如 `assets/schemas/`, `references/` 等） | 80% |

**风险**：仓库内容列表未完全覆盖代码结构。

**建议**：补充 `assets/schemas/`, `references/`, `scripts/` 等路径。

---

## 三、代码实现超出文档的部分（正向偏差）

| # | 代码实现 | 文档覆盖 | 评价 |
|---|----------|----------|------|
| 1 | `StoryReadAccessSkill` / `StoryWriteAccessSkill` | ❌ 未提及 | 代码能力超出文档描述 |
| 2 | `LocalStoryAssistAccessSkill`（读取→处理→写回） | ❌ 未提及 | 代码能力超出文档描述 |
| 3 | 8个第三层接口（实际实现） | ⚠️ 只列2个 | 代码能力超出文档描述 |
| 4 | 脱敏输出（`none/partial/full`） | ❌ 未提及 | 代码能力超出文档描述 |
| 5 | 预算控制（读写预算扣减） | ❌ 未提及 | 代码能力超出文档描述 |
| 6 | `requestStoryRead` / `requestStoryWrite` 等接口 | ❌ 未提及 | 代码能力超出文档描述 |
| 7 | `ChallengeSigningAuthorizationSkill`（实际签名执行） | ✅ 英文版提及 | 代码能力部分覆盖 |

---

## 四、建议修正清单（v4.0 优先级）

### P0（阻塞性，必须修复）

1. [ ] **统一第三层接口描述**：文档补充8个接口，或代码移除未使用的6个接口
2. [ ] **统一第二层技能描述**：文档补充 `StoryReadAccessSkill`, `StoryWriteAccessSkill`, `LocalStoryAssistAccessSkill`，或代码移除这些技能
3. [ ] **补充安全机制**：文档补充脱敏输出和预算控制

### P1（重要，建议尽快修复）

4. [ ] **统一应用场景**：场景描述更具体化，直接对应代码能力（登录授权、签名授权、密码填充、故事处理）
5. [ ] **更新产品特性**：特性四改为"保留WASM集成空间"，或代码中增加Pharos集成证据
6. [ ] **核对自测覆盖**：核对 `selftest.mjs` 实际覆盖，更新文档描述
7. [ ] **更新英文版技能列表**：统一中英文技能列表与代码一致

### P2（优化，可后续迭代）

8. [ ] **更新扩展方向**：扩展方向更新为已实现能力，重新定义后续方向
9. [ ] **补充仓库内容列表**：补充 `assets/schemas/`, `references/`, `scripts/` 等路径

---

## 五、结论

### 总体评价

StoryLock 文档与代码一致性为 **75%**，存在较严重的偏差：

1. 🔴 **文档严重低估项目能力**：文档只列2个第三层接口，代码实现8个；文档只列3个第二层技能，代码实现6个；文档缺失脱敏和预算控制描述。
2. 🔴 **文档与代码架构不匹配**：文档描述的是"九宫格验证授权"架构，但代码还保留了"故事对象读写"架构，两者并存导致混乱。
3. 🟡 **场景描述过于抽象**：4个策略探索型场景未直接对应代码中的具体能力。

### 核心问题

**文档和代码是两个不同的架构**：
- **文档架构**：聚焦九宫格验证授权（`ObjectStrengthPolicySkill`, `GridChallengeSkill`, `LocalAuthorizationSkill`）
- **代码架构**：在九宫格验证授权基础上，还保留了故事对象读写（`StoryReadAccessSkill`, `StoryWriteAccessSkill`, `LocalStoryAssistAccessSkill`）和8个第三层接口

**建议**：
- **方案A**：文档升级，完整覆盖代码能力（推荐）
- **方案B**：代码精简，移除文档未提及的技能和接口，聚焦九宫格验证授权

---

*评审完成时间：2026-06-17 08:15 GMT+8*  
*评审人：代码安全审计师*  
*版本：v4.0（基于最新代码和文档重新评审）*
